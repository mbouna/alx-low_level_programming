#include <stdio.h>
#include <stdlib.h>
/**
 * main
 * Description: Get a random number and check its last digit, finally compare it with 5
 * Return: 0
 */
int main(void)
{
	char c = 'a';

	while (c <= 'z')
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
